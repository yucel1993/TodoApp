apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "todomysqlchart.fullname" . }}-test-connection"
  labels:
    {{- include "todomysqlchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "todomysqlchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
