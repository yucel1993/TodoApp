apiVersion: v1
kind: Service
metadata:
  name: {{ include "todomysqlchart.fullname" . }}
  labels:
    {{- include "todomysqlchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todomysqlchart.selectorLabels" . | nindent 4 }}
